<script src="{{ 'wishlist.js' | asset_url }}" defer="defer"></script>
{{ 'product.css' | asset_url | stylesheet_tag }}
{{ 'wishlist.css' | asset_url | stylesheet_tag }}
{%- liquid
  assign th_st = settings
  assign container = th_st.general_layout
  assign product_style = th_st.product_style
-%}

<section
  class="bls__section bls__product bls__wishlist-page-section"
  data-section-type="page-wishlist"
  data-section-id="{{ section.id }}"
  data-view="{{ template.name }}"
>
  <div class="bls__wishlist-page-section-inner">
    <div class="{{ section.settings.layout }} bls__wishlist-page-main bls__grid {{ product_style }}">
      {%- if search.results != blank -%}
        {% paginate search.results by 200 %}
          <div class="row">
            {%- for item in search.results -%}
              <div
                class="col-6 col-md-4 col-lg-3 bls__product-load bls__wl-product-js bls__product-preload-js bls__wishlist-list"
                data-product-handle="{{ item.handle }}"
              >
                {%- render 'product-item' | product: item | is_wishlist_page: true -%}
              </div>
            {%- endfor -%}
          </div>
        {% endpaginate %}
      {%- endif -%}
      <div class="bls__wishlist-no-product-js d-none">
        <h3 class="bls__wishlist-no-product-text">
          {{ 'templates.wishlist.no_product' | t }}
          <a class="bls__wishlist-no-product-url" href="/collections/all">{{ 'templates.wishlist.redirect' | t }}</a>
        </h3>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Content container",
  "settings": [
    {
      "type": "header",
      "content": "Container"
    },
    {
      "type": "select",
      "id": "layout",
      "label": "Layout",
      "default": "container",
      "options": [
        {
          "value": "container",
          "label": "Default"
        },
        {
          "value": "container-fluid",
          "label": "Fluid container"
        },
        {
          "value": "full-width",
          "label": "Full width"
        }
      ]
    }
  ]
}
{% endschema %}
